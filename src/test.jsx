import { oldMethod } from '../utils';

const test = () => {
  const name = 'negtar';

  oldMethod();

  return (
    <div>
      <div>{name}</div>
      لورم ملیسشنتبشسکیتب مسبنیتکشسبیتن
    </div>
  );
};

export default test;
